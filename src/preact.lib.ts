import {
  Component,
  Fragment,
  h,
  render
} from 'preact';

import {
  useState
} from 'preact/hooks';

export const preact = { Component, Fragment, h, render };
export const hooks = { useState };
